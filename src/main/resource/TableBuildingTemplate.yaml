config:
  database: database name
  schema: schema name
table:
- name: table name
  column:
  - name: column name
    type: data type
    required: Is the column value required
    defaultVal: default value for the column
    note: note for the column
    valueEnum:
    - value: value
      desc: description
  - name: column name
    type: data type
    required: Is the column value required
    defaultVal: default value for the column
    note: note for the column
    valueEnum:
    - value: value
      desc: description
  constraint:
  - name: constraint name
    type: constraint type, e.g. PRIMARY KEY
    column:
    - column name of primary key
    - column name of primary key
  - name: constraint name
    type: constraint type, e.g. PRIMARY KEY
    column:
    - column name of primary key
    - column name of primary key
  index:
  - name: index name
    type: index type, e.g. UNIQUE
    column:
    - name: column name of index
      order: access order of index